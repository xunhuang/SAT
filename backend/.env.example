# Server Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:5173

# Firebase Admin Configuration
# You will need to generate a service account key from the Firebase console
# and save it as serviceAccountKey.json in the config directory
FIREBASE_DATABASE_URL=https://sattest-eaadc.firebaseio.com

# Email Configuration
# These settings are used for sending email notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-app-password
EMAIL_FROM=sat-practice@example.com

# For Gmail, you'll need to use an "App Password" if you have 2FA enabled
# Go to Google Account > Security > App Passwords to generate one
# For other email providers, use the appropriate SMTP settings